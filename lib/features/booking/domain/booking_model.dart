// Phase 1 — immutable Booking model with JSON (de)serialization.
// Fields mirror the bookings table in §4 of the spec.
// Pricing is never stored directly here; it is computed by PricingEngine (Phase 2+).
